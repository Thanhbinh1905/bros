# Build Context

## Implementation Notes

The build creates a dependency-free scaffold and imports assets only from approved OpenCode asset directories.

## Constraints

No raw OpenCode config was authorized for import. No package install, publish, deploy, or live configuration mutation is part of this task.
